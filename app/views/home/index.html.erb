<div class="flex flex-col items-center w-full max-w-md mx-auto  bg-white shadow-lg rounded-lg p-6 py-2">
    <div class="w-full">
        <div class="py-2 w-full flex justify-between items-center">   
            <h1 class="text-2xl font-semibold flex items-center text-black" data: { testid: "home-index-title" }>🏠 สินค้าทั้งหมด</h1>
            <% if @user %>
                <span class="text-gray-600" data: { testid: "home-index-user-email" }><%= @user.email %></span>
            <% end %>
        </div>
            <div id="products" class="mt-8">
                <% if @products.any? %>
                    <% @products.each_slice(2) do |product_pair| %>
                        <div class="flex justify-between gap-8 mb-8">
                            <% product_pair.each do |product| %>
                                <div class="bg-white rounded-lg shadow-md w-46">
                                    <%= render "products/product", product: product %>
                                </div>
                            <% end %>
                            <% if product_pair.length == 1 %>
                                <div class="bg-white rounded-lg shadow-md w-46 invisible">
                                </div>
                            <% end %>
                        </div>
                    <% end %>
                <% else %>
                    <p class="text-center my-10" data: { testid: "home-index-no-products-text" }>ไม่มีสินค้า</p>
                <% end %>
            </div>
        </div>

        <% content_for :footer do %>
            <%= render "bottom/footer" %>
        <% end %>
    </div>
</div>


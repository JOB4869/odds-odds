<div class="flex flex-col items-center w-full max-w-md mx-auto bg-white shadow-lg rounded-lg p-6 py-2">
  <div class="w-full">
    <div class="py-2 w-full">
      <h1 class="text-2xl font-semibold flex items-center">
        <span class="mr-2">
          ✏️
        </span>
        บัญชีผู้ใช้
      </h1>
    </div>

    <% if @user.nil? || (@user.first_name.blank? && @user.last_name.blank? && @user.address.blank?) %>
      <div class="py-8 text-center">
        <p class="text-gray-500 mt-8 mb-4">ยังไม่มีข้อมูล</p>
        <%= link_to "เพิ่มข้อมูล", edit_account_path(@user), class: "w-full flex justify-center bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 mt-8 mb-4" %>
        <%= link_to "กลับสู่หน้าบัญชีผู้ใช้", users_path, class: "block w-full text-center mt-4 text-gray-600 py-2 px-4 rounded-md bg-gray-100 hover:bg-gray-200" %>
      </div>
    <% else %>
      <div class="py-4 space-y-4">
        <div class="border-b pb-2">
          <p class="text-gray-600">ชื่อ:</p>
          <p class="mt-1"><%= @user.first_name %></p>
        </div>

        <div class="border-b pb-2">
          <p class="text-gray-600">นามสกุล:</p>
          <p class="mt-1"><%= @user.last_name %></p>
        </div>

        <div class="border-b pb-2">
          <p class="text-gray-600">ที่อยู่:</p>
          <p class="mt-1"><%= @user.address %></p>
        </div>

        <div class="border-b pb-2">
          <p class="text-gray-600">โทรศัพท์:</p>
          <p class="mt-1"><%= @user.phone %></p>
        </div>

        <div class="border-b pb-2">
          <p class="text-gray-600">พร้อมเพย์:</p>
          <p class="mt-1"><%= @user.prompt_pay %></p>
        </div>

        <div class="mt-6 space-y-4">
          <%= link_to "แก้ไขข้อมูล", edit_account_path(@user), class: "w-full block text-center bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700" %>
          <%= link_to "กลับสู่หน้าบัญชีผู้ใช้", users_path, class: "block w-full text-center text-gray-600 py-2 px-4 rounded-md bg-gray-100 hover:bg-gray-200" %>
        </div>
      </div>
    <% end %>
  </div>
</div>

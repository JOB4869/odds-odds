<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "ODDS ODDS SHOP" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="<%= asset_path 'PICTURE.svg' %>" type="image/svg+xml">
    <link rel="apple-touch-icon" href="<%= asset_path 'PICTURE.svg' %>">
    <link href="https://cdn.tailwindcss.com" rel="stylesheet">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="header-container" data-controller="logo">
      <%= render partial: "header/logo" %>
    </div>
    <%= turbo_frame_tag "modal" %>
    <main class="container mx-auto flex content">
      <%= render partial: "header/flash" if lookup_context.exists?("header/_flash") %>
        <%= yield %>
    </main>
    <div class="bottom-container" data-controller="logo">
      <%= yield :footer %>
      <%= yield :button_product %>
      <%= yield :back_home %>
      <%= yield :back_users %>
    </div>
  </body>
</html>
